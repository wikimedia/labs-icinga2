#!/bin/sh
template=`cat <<TEMPLATE
***** Icinga  *****

Notification Type: $NOTIFICATIONTYPE

Host: $HOSTALIAS
Address: $HOSTADDRESS
State: $HOSTSTATE

Date/Time: $LONGDATETIME

Additional Info: $HOSTOUTPUT

Comment: [$NOTIFICATIONAUTHORNAME] $NOTIFICATIONCOMMENT
TEMPLATE
`

# /usr/bin/printf "%b" "$template" | mail -s "$NOTIFICATIONTYPE - $HOSTDISPLAYNAME is $HOSTSTATE" $USEREMAIL

/bin/echo "$NOTIFICATIONTYPE - Host $HOSTALIAS is $HOSTSTATE: $HOSTOUTPUT $HOSTACKAUTHOR $HOSTACKCOMMENT $NOTIFICATIONAUTHORNAME $NOTIFICATIONCOMMENT" >> /var/log/icinga2/irc.log

